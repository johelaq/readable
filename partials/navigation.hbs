<ul class="nav masthead-nav">
	{{#foreach navigation}}
	<li class="nav-{{label}} {{#if current}}active{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
	{{/foreach}}
</ul>
